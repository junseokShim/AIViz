# AIViz dependencies
# Install: pip install -r requirements.txt

# Desktop UI
PyQt6>=6.6.0

# Plotting (embedded in PyQt)
matplotlib>=3.8.0

# Data handling
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0
pyarrow>=14.0.0

# Signal analysis
scipy>=1.11.0

# Advanced signal analysis: CWT (optional, recommended)
# If not installed, CWT analysis is disabled with a clear UI message.
PyWavelets>=1.4.0

# Image analysis
Pillow>=10.0.0

# HTTP client for Ollama
httpx>=0.25.0

# Forecasting
statsmodels>=0.14.0

# Export
fpdf2>=2.7.0

# Utilities
python-dotenv>=1.0.0

# Optional but recommended: OpenCV for Canny edge detection
# If not installed, scipy-based fallback is used automatically.
# opencv-python-headless>=4.8.0

# Optional: scikit-learn for K-Means image segmentation and clustering
scikit-learn>=1.3.0

# Database integration
# SQLite: always available via Python's built-in sqlite3 (no extra install needed)
#
# Optional: SQLAlchemy for MySQL / PostgreSQL support
# pip install sqlalchemy pymysql psycopg2-binary
# SQLAlchemy>=2.0.0
# pymysql>=1.1.0
# psycopg2-binary>=2.9.0

# Dev / test
pytest>=8.0.0
